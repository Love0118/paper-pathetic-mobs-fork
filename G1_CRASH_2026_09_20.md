# 2026-09-20 JVM 크래시 조사

운영 hs_err_pid4232.log: Microsoft OpenJDK 25.0.3+9, 22:50:06, g1HeapRegionManager.cpp:55의 master free list MT safety protocol at a safepoint 보장 조건 실패. 현재 스레드는 VM Periodic Task Thread. 사용 힙 약 1.74GiB/최대 4GiB, 시스템 여유 약 12.6GiB. 메모리 고갈로 확인되지 않았습니다.

로컬 JVM 기본값 조회 결과 G1UseTimeBasedHeapSizing=true(diagnostic/default), G1UncommitDelayMillis=300000, G1MinRegionsToUncommit=10입니다. 크래시 보고서의 평가 간격은 60초입니다. G1PeriodicGCInterval=0이며 periodic GC는 꺼져 있습니다. 주기적 GC와 시간 기반 힙 반환은 다른 기능입니다.

[Microsoft 이슈 677의 원인 설명](https://github.com/microsoft/openjdk/issues/677#issuecomment-5199140538)은 동일한 파일·행·assertion을 Microsoft 전용 시간 기반 heap sizing의 타이밍 경쟁으로 설명합니다. GC와 독립적으로 실행되는 축소/uncommit 경로가 safepoint 중 free-list 동기화 조건을 지키지 않아 abort할 수 있다는 내용입니다. 댓글은 동기화 수정과 회귀 테스트, 차기 Java 25 업데이트 반영 예정이라고 설명하지만 정확한 수정 배포 버전은 명시하지 않습니다.

이는 Paper의 presentation mob tick, retained frames, prefix 최적화가 구현한 기능이 아니라 JDK 내부 기능입니다. 해당 JDK release SOURCE 7a05ec815bae의 정확한 공개 소스 리비전은 이번 조회에서 확보하지 못했으므로 수정 코드를 직접 검증했다고 주장하지 않습니다. 기본 OpenJDK g1HeapRegionManager.cpp의 검사에서는 safepoint일 때 VM thread 또는 FreeList_lock 소유를 요구합니다.

공개 설명의 권장 우회 옵션:

```text
-XX:+UnlockDiagnosticVMOptions -XX:-G1UseTimeBasedHeapSizing
```

설치된 동일 JVM에서 이 옵션과 PrintFlagsFinal을 실행해 false(command line)로 적용됨을 확인했습니다. 이 조사는 운영 시작 스크립트의 GC 옵션을 변경하지 않습니다. 적용하면 Microsoft의 시간 기반 idle heap 반환을 끄고 일반 G1 heap sizing으로 돌아갑니다. G1 전체나 일반 GC를 끄는 옵션이 아닙니다.

## 함께 완료한 서버 업데이트

- Paper 26.3 build 26의 소스 a2551859805030dd1986d412cdc4f587fdddbc0b 병합.
- Rust JNI/FFM 소스·로더·CI 제거. Java 전투와 guarded Java entity batch 유지.
- Gradle 패치 적용, MUD 서버 테스트 6개 및 Paperclip 빌드 성공.
- 새 서버에서 49종 적·67개 바이옴·220틱 이동/제거, 플러그인 210개 테스트, Via 1.21.8 BGM/게임 기능, 업적 50종·재시작 영속성 검증 통과.
- 서버 JAR SHA-256: 2c4920b64b6848aae1dd624be7937e7195fd7fa294b27d59788c32fef0337eb7.
- 운영 서버가 정지된 상태에서 JAR 교체, Rust DLL과 이전 파일은 backups/paper26-java-only에 보존. 재시작하지 않았습니다.

Paper 갱신과 Rust 제거는 위 JVM 동기화 버그를 수정하는 작업과 별개입니다. 장시간 동일 부하에서 재발하지 않는다는 검증은 아직 하지 않았습니다.
