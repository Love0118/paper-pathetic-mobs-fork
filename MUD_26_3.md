# MUD 전용 Paper 26.3

이 브랜치는 Paper 26.3 build 19의 소스 커밋 b4c7d1686d에서 시작합니다. 기존 beta(26.2) 브랜치와 독립적이며 26.2 패치 전체를 이식하지 않습니다. 변경·벤치마크는 AI 코딩 도구로 작성하고 로컬 테스트했습니다. Paper 공식 배포물이 아닙니다.

## 선택한 최적화

config/paper-global.yml에 다음 항목이 생성됩니다. 기본값은 모두 false입니다.

~~~yaml
mud-optimizations:
  presentation-mob-tick: true
  retained-frames: true
  in-place-frame-prefix: true
~~~

네트워크 옵션은 채널 생성 시 읽으므로 변경 후 재시작해야 합니다.

- presentation-mob-tick: mud_presentation_v1 태그와 AI 꺼짐·물리 꺼짐·중력 꺼짐·무적·생존·탑승자 없음 조건을 모두 만족하는 Mob만 바닐라 게임플레이 틱을 건너뜁니다. commonTick(나이·이전 위치 갱신), 월드 등록·추적·메타데이터 전송은 유지합니다. 플러그인이 전투·체력·이동·제거를 전부 책임지는 외형 엔티티 전용 계약입니다. 일반 몹과 플레이어에는 적용하지 않습니다.
- mudMovePresentation(Location): 위 조건을 만족하는 엔티티의 이미 로드된 같은 월드 청크 안에서 연속 이동을 수행합니다. 일반 teleport API와 별개이며 EntityTeleportEvent를 호출하지 않습니다. 플러그인의 경로 이동만 이 API를 사용하고 좌클릭 재배치는 기존 teleport를 사용합니다. 조건에 맞지 않으면 false를 반환해 플러그인이 일반 경로로 돌아갑니다.
- retained-frames: 프레임과 압축하지 않은 payload의 readBytes 복사 대신 readRetainedSlice를 사용합니다. 완전한 프레임을 받은 후에만 적용합니다.
- in-place-frame-prefix: 패킷/압축 출력에 3바이트 여유 공간을 예약하고 길이 prefix를 직접 기록합니다. refCnt=1, 충분한 여유 공간, 읽기/쓰기 가능, 단일 버퍼만 허용합니다. 공유·slice·composite·read-only 버퍼는 복사로 돌아갑니다. 패킷 자체의 codec 직렬화나 압축을 생략하지 않습니다.

프레임 처리는 기존 26.2 포크의 0037/0039에서 선택 이식했으며 26.3 파일의 기존 동작을 유지했습니다. ProtocolLib 같은 외부 패킷 변환 플러그인 조합은 별도 검증 대상입니다.

## 이식하지 않은 기존 최적화

| 항목 | 판단 |
|---|---|
| Pathetic 2D 경로·공유 셀·역방향 flow field | MUD는 AI를 끄고 고정 순환 경로를 자체 계산하므로 호출되지 않음 |
| managed mob AI 간격 조절 | AI가 이미 꺼져 있어 이득 없음 |
| managed Bukkit damage·hurt status 통합 | MUD는 코어에서 체력을 계산하며 LivingEntity.damage를 호출하지 않음 |
| 추가 PLAY 큐·flush batching | 26.3의 기존 Connection 큐·flush consolidation 위에 추가하는 기능이며 완전히 중복되지는 않음. 이번 프로파일의 지배적 비용이 아니어서 이식 보류 |
| viewer별 LOD·effect 통합 | 개인 전장 시야 분리 및 플러그인 타격 효과 중복 제거 우선. LOD로 보이는 이동 품질을 바꾸는 별도 검증 필요 |
| dense block update·폭발 수신자 탐색 | 진행 중 대량 블록 변경/폭발이 없는 MUD 워크로드에 해당하지 않음 |
| Velocity 압축·암호화 | Paper 26.3에 이미 포함. 다시 이식하지 않음. Windows에서는 Java fallback 사용을 실제 로그에서 확인 |
| Moonrise 엔티티/청크 추적·GoalSelector 최적화 | Paper 26.3에 이미 포함. 26.2 포크 전체 덮어쓰기 대신 현재 기반을 유지 |

26.3에 없는 것과 MUD에서 쓸 필요가 없는 것은 구분했습니다. 26.3에서 feature patch가 삭제됐다는 이유만으로 전부 바닐라에 흡수됐다고 가정하지 않습니다. 비교 기준은 build 19이며 이후 빌드의 변경은 별도 확인해야 합니다.

## 빌드와 검증

~~~powershell
.\gradlew.bat applyPatches
.\gradlew.bat :paper-server:test --tests '*Mud*' createPaperclipJar
~~~

결과는 paper-server/build/libs/paper-paperclip-26.3.local-SNAPSHOT.jar입니다. 네트워크 테스트는 VarInt 경계, 버퍼 공유/복사 fallback, 압축 on/off 왕복, 나뉜 프레임 수신·연속 프레임·retain 후 입력 해제를 확인합니다. 엔티티 테스트는 모든 적용 조건을 확인합니다.

실서버 벤치마크와 원본 수치는 연결된 mc-luck-defense 저장소의 docs/performance/REPORT.md에 기록합니다. 로컬 프로토콜 클라이언트 20개가 실제 TCP 연결·압축·패킷 수신을 수행하지만 게임 화면을 렌더링하지는 않습니다. 원격 네트워크 지연, 온라인 인증 암호화, 실제 Minecraft 클라이언트 FPS 검증은 포함하지 않습니다.

Rust는 이번 프로파일에서 도입하지 않았습니다. 지배적인 비용은 바닐라 틱/추적/반복 이동이어서 불필요한 작업을 제거했습니다. Java 객체와 월드 접근이 대부분인 경로를 JNI로 옮기면 경계 비용과 유지보수 비용을 추가하면서 같은 작업은 남습니다.

## Build 26 / Java 전용 업데이트

Paper 26.3 build 26 (a2551859805030dd1986d412cdc4f587fdddbc0b)을 병합했습니다. Rust JNI 소스·로더·워크플로를 제거하고 mudMovePresentationBatch는 조건을 전체 검증한 뒤 Java에서 위치·회전을 갱신합니다. MUD DLL과 mud.native 실행 옵션은 필요하지 않습니다. 기존 Java presentation tick 및 프레임 최적화는 유지합니다. 위 build 19 비교는 초기 이식 판단의 기록입니다.
